[project]
name = "ostiarius-face-sidecar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "numpy>=2.0", "opencv-python-headless>=4.10", "onnxruntime>=1.19", "mediapipe>=0.10.14", "python-multipart>=0.0.12"]

[project.optional-dependencies]
test = ["pytest>=8", "httpx>=0.27"]

[tool.pytest.ini_options]
pythonpath = ["."]
